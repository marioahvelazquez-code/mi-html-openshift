services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    expose:
      - "8000"
    ports:
      - "8001:8000"
    env_file:
      - .env

  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
    ports:
      - "8090:80"
    depends_on:
      - backend
